import { GendersearchPipe } from './gendersearch.pipe';

describe('GendersearchPipe', () => {
  it('create an instance', () => {
    const pipe = new GendersearchPipe();
    expect(pipe).toBeTruthy();
  });
});
